---
import Layout from '../../layouts/Layout.astro'
import { type CollectionEntry, getCollection } from 'astro:content'
import WorkCard from '../ui/WorkCard.astro'
import { clientProjectsKey } from '../../content/config'
import { getLangFromUrl } from '../../i18n/utils'

const lang = getLangFromUrl(Astro.url)

const clientProjects: CollectionEntry<'client-projects'>[] = await getCollection(clientProjectsKey)
---


<Layout title="Work - Jo Maendle Photography">
	<section class="card mx-auto max-w-[72rem] flex h-full flex-col gap-8">
		<div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-3 justify-center pt-16">
			{
				clientProjects.map((project) => (
					<a href=`/${lang}/details/${project.slug}` class="animated-card sm:aspect-[2/3] aspect-square max-h-[480px] sm:max-h-none">
						<WorkCard project={project} />
					</a>
				))
			}
		</div>
	</section>
</Layout>
